import React from 'react';

import { MainApi, MainView } from 'entities/main';
import { useInput, useRouter, useStorage, useYup } from 'shared/hooks';

function Main() {
    return <MainView />;
}

export default Main;
